let t = true;let f = false;func isPrime(n) {	if n <= 1 { f; };	if n == 2 { t; };	if (n % 2) == 0 { f; };	/* Divisor */	let d = 3;	while (d * d) <= n {		if (n % d) == 0 { f; };		d = d + 2;	};	t;};func primeCount(max) {	print max;	let out = 0;	let num = 2;	while num < max {		let prime = isPrime(num);		if prime {			out = out + 1;		};		print num;		print out;		num = num + 1;	};	out;};let limit = 50;let programOut = primeCount(limit);programOut;